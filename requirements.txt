# Core
numpy>=1.24
pandas>=2.0
scipy>=1.10
scikit-learn>=1.3
joblib>=1.3
tqdm>=4.65
openpyxl>=3.1          # xlsx export of metrics

# Models
torch>=2.0             # SimpleMLP; install the CUDA build matching your driver
xgboost>=2.0

# Cheminformatics
rdkit>=2023.3
descriptastorus>=2.6   # required by GROVER's rdkit_2d_normalized features

# GROVER itself is NOT a pip package. Clone it separately:
#   git clone https://github.com/tencent-ailab/grover external/grover
#   pip install -r external/grover/requirements.txt

# Optional -- only needed to generate Mordred or PaDEL descriptors for NEW molecules.
# The dataset archive ships the caches used in the paper, so reproduction does not
# need these. PaDEL additionally requires a Java runtime.
#   pip install scikit-fingerprints padel-pywrapper
